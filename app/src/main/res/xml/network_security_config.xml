<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- App-wide base config allowing cleartext for development -->
    <base-config cleartextTrafficPermitted="true" />

    <!-- Explicit domain allowances (optional) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.94.163</domain>
    </domain-config>
</network-security-config>
