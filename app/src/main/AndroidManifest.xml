<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ==================== PERMISSIONS ==================== -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    -->

    <application
        android:name=".FilmSpaceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmSpace_Mobile"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ==================== VIDEO PLAYER ==================== -->
        <activity
            android:name=".ui.movie.VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <!-- ==================== MAIN ACTIVITY (LAUNCHER) ==================== -->
        <activity
            android:name=".ui.test.TestLauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <!-- ==================== SETTINGS (CHƯA TEST) ==================== -->
        <!--
        <activity
            android:name=".ui.setting.SettingActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.ChangePasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.NotificationsActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.SecurityActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.HelpSupportActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.LegalPoliciesActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />

        <activity
            android:name=".ui.setting.LanguageActivity"
            android:exported="false"
            android:theme="@style/Theme.FilmSpace_Mobile.NoActionBar" />
        -->

        <!-- ==================== OTHER SCREENS ==================== -->
        <activity android:name=".ui.splash.SplashActivity" />
        <activity android:name=".ui.onboarding.OnboardingActivity" />
        <activity android:name=".ui.movie.MovieDetailActivity" />
        <activity android:name=".ui.movie.MoviesByGenreActivity" />
        <activity android:name=".ui.auth.AuthActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.subscription.SubscriptionDescriptionActivity" />
        <activity android:name=".ui.subscription.PaymentMethodActivity" />
        <activity android:name=".ui.subscription.PaymentReturnActivity" />
        <activity android:name=".ui.subscription.PaymentSuccessActivity" />

    </application>

</manifest>